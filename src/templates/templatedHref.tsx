import * as E from '../elements';
import { h } from 'preact';

interface TemplatedHrefProps {
  element: E.TemplatedHrefElement;
}

export function TemplatedHref(props: TemplatedHrefProps) {
  return (
    <div class="element-templatedHref">
    </div>
  )
}